; ------------------------------ Portable Applications Installer -------------------------------
; INI file format describtion: https://en.wikipedia.org/wiki/INI_file
; 
; Valid command-line options(case sensitive):
; /A=sections - INI sections (separated by dlm) to process automatically
; /C=path - use custom INI file. If it's not specified or missing the default INI file in installer directory with installer name and installer section will be created and used
; /D=path - set installation directory(INSTDIR). It must be the last parameter used in the command-line and must not contain any quotes, even if the path contains spaces. Only absolute paths are supported
; /U - update existing source files(src) and program directories or files(upd)
; /S - run silently, do not show installation progress and details
; Options /A=, /D=, /dlm= overriding installer section properties of the INI file
; 
; Valid properties for the installer section of the INI file:
; dir - installation directory(INSTDIR), default: %TEMP%
; dir=[locate]path - search for path on all drives, if not found %TEMP%\path will be used
; dlm - delimiter for lists of items, default: ";"
; aut - INI sections (separated by dlm) to process automatically
; 
; Valid properties for the app section of the INI file:
; trg - program directory or file to check if program is already installed
; upd - program directory or file to update
; src - installation source file
; url - download url, HTTP and FTP protocols
; coo - cookies to use in request header
; unz - extract to directory
; ren - rename to section name directory
; ex# - commands to execute, e.g. ex1, ex2 etc.
; 
; Valid variables for the INI values and command-line options:
; Windows Environment Variables, see: https://en.wikipedia.org/wiki/Environment_variable
; %LOCALAPPDATA% - included in Windows Vista, usually C:\Users\[username]\AppData\Local
; %DOCUMENTS% - documents directory, usually C:\Users\[username]\Documents
; %DOWNLOADS% - downloads directory, usually C:\Users\[username]\Downloads
; %EXEDIR% - directory containing installer executable
; %INSTDIR% - installation directory, can be used as root directory for stored program files
; %INIDIR% - INI file directory, can be used as root directory for stored program settings
; %ROOT% - root directory(drive) of installer executable
; ------------------------------ installer section -----------------------------
[pai]
dir=%TEMP%\wtools
; ------------------------------- apps sections --------------------------------
; Use 7z and innounp to extract the contents of a setup file
; [src].unp directory will be automaticaly removed after install
[7z]
trg=%INSTDIR%\7z\7z.exe
upd=%INSTDIR%\7z
src=%INSTDIR%\7z.msi
url=http://downloads.sourceforge.net/sevenzip/7z1604.msi
ex1="msiexec /a "%INSTDIR%\7z.msi" /qn TARGETDIR="%INSTDIR%\7z.msi.unp""
ex2="cmd /c xcopy /e/h/i/r/y "%INSTDIR%\7z.msi.unp\Files\7-Zip" "%INSTDIR%\7z\""
[innounp]
trg=%INSTDIR%\innounp\innounp.exe
upd=%INSTDIR%\innounp
src=%INSTDIR%\innounp.rar
url=http://downloads.sourceforge.net/innounp/innounp040.rar
ex1=""%INSTDIR%\7z\7z.exe" x -y -o"%INSTDIR%\innounp" "%INSTDIR%\innounp.rar""
